import { Block } from './Block.js';

export const BlocksPage = () => {
    return [
        Block({title: 'Another headline', subtext: 'And an even wittier subheading.'}),
        Block({title: 'Another headline', subtext: 'And an even wittier subheading.'}),
        Block({title: 'Another headline', subtext: 'And an even wittier subheading.'}),
        Block({title: 'Another headline', subtext: 'And an even wittier subheading.'})
    ]
}